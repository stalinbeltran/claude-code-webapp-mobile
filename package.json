{
  "name": "claude-code-webapp-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web de lectura para las conversaciones del ejecutor `c` del coordinador de Telegram.",
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
